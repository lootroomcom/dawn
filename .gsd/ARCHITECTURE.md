# Architecture

> Auto-generated by /map on 2026-03-22

## Overview

This project is a Shopify Online Store 2.0 theme based on the **Dawn** reference theme (v15.4.1). It follows the standard Shopify theme structure where Liquid templates and sections define the layout and functionality.

```
┌─────────────────────────────────────────┐
│            [Layouts (theme.liquid)]     │
├─────────────────────────────────────────┤
│         [Templates (JSON/Liquid)]       │
├─────────────────────────────────────────┤
│         [Sections (*.liquid)]           │
├─────────────────────────────────────────┤
│         [Snippets (*.liquid)]           │
└─────────────────────────────────────────┘
```

## Components

### Core Layout
- **Purpose:** Global wrappers for the storefront.
- **Location:** `layout/theme.liquid`
- **Integrations:** Trustpilot widget, global JS/CSS assets.

### Custom Sections
- **chic-multi-columns:** Likely a custom implementation of a multi-column layout.
- **breadcrumbs:** Navigation aid.
- **Featured Product / Main Product:** Core product display logic with identified technical debt (TODOs).

### Assets
- **Swiper.js:** Used for sliders/carousels (detected `assets/swiper-bundle.min.js`).
- **Standard Dawn Assets:** `global.js`, `constants.js`, `theme.css`.

## Data Flow

1. **Request:** Shopify server receives a request for a page (index, product, collection).
2. **Layout:** The server wraps the content in `layout/theme.liquid`.
3. **Template:** The specific JSON or Liquid template in `templates/` determines which sections are rendered.
4. **Sections:** Sections in `sections/` fetch data via the Shopify `section` and `block` objects.
5. **Snippets:** Sections include reusable logic from `snippets/`.
6. **Client-side:** Assets in `assets/` provide interactive behavior.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Trustpilot | Script | Customer reviews widget |
| Swiper.js | Library | Carousel/slider functionality |

## Technical Debt

- [ ] Clear TODOs in `sections/featured-product.liquid` regarding variable verbosity and `theme-check`.
- [ ] Clear TODOs in `sections/main-product.liquid` regarding `theme-check`.
- [ ] Potential inconsistency in JS loading (some deferred, some async like Trustpilot).

## Conventions

**Naming:** Standard Shopify snake_case for sections/snippets; kebab-case for assets.
**Structure:** Standard Shopify theme structure.
**Testing:** No automated tests detected (standard for Shopify themes without a build pipeline).
