---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Template Modernization

## Objective
Convert the legacy `gift_card.liquid` template to a modern JSON-based template system to align with Shopify OS 2.0 best practices and enable section-based customization.

## Context
- .gsd/SPEC.md
- templates/gift_card.liquid
- sections/main-gift-card.liquid

## Tasks

<task type="auto">
  <name>Extract gift card logic to new section</name>
  <files>
    templates/gift_card.liquid
    sections/main-gift-card.liquid
  </files>
  <action>
    - Create `sections/main-gift-card.liquid`.
    - Copy the Liquid logic and HTML structure from `templates/gift_card.liquid` to the new section.
    - Ensure all assets and snippets used in the section are correctly referenced.
  </action>
  <verify>Check sections/main-gift-card.liquid for valid Liquid syntax.</verify>
  <done>File exists and contains the gift card logic.</done>
</task>

<task type="auto">
  <name>Create JSON template and delete legacy file</name>
  <files>
    templates/gift_card.json
    templates/gift_card.liquid
  </files>
  <action>
    - Create `templates/gift_card.json` with a single section "main" of type "main-gift-card".
    - Delete `templates/gift_card.liquid`.
  </action>
  <verify>Check if templates/gift_card.json exists and templates/gift_card.liquid is gone.</verify>
  <done>Templates directory is modernized.</done>
</task>

## Success Criteria
- [ ] No `.liquid` templates remaining in the `templates/` root (excluding `templates/customers/` if applicable, though we saw they are already JSON).
- [ ] `gift_card` page architecture follows OS 2.0 standards.
