# Plan 1.2 Summary: Style Debt Removal

## Objective
Eliminate hardcoded `!important` overrides in `assets/custom.css`.

## Changes
- Updated `config/settings_schema.json` with new "Global Overrides" section:
    - `body_bg_position`, `body_bg_size`, `body_bg_repeat`.
    - `header_menu_color`, `header_menu_weight`.
- Updated `layout/theme.liquid` to define corresponding CSS variables in the `:root` block.
- Refactored `assets/custom.css`:
    - Removed `!important` from `body` background settings and mapped to new variables.
    - Removed `!important` from header menu item styles and mapped to new variables.
    - Removed `!important` from `.shopify-section:has(.footer-wrapper)` overrides.

## Verification
- Grep test: `assets/custom.css` now contains ZERO `!important` tags for layout or branding overrides.
- Theme Check: Verified that new variables are correctly used.

## Outcome
PASS. Technical debt from style overrides is removed.
