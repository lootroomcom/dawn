---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Style Debt Removal

## Objective
Eliminate hardcoded `!important` overrides in `assets/custom.css` and move them to dynamic Shopify Schema settings in `config/settings_schema.json` to improve maintainability and follow Theme Store standards.

## Context
- .gsd/SPEC.md
- config/settings_schema.json
- assets/custom.css

## Tasks

<task type="auto">
  <name>Add Schema settings for hardcoded styles</name>
  <files>config/settings_schema.json</files>
  <action>
    - Add new settings to `settings_schema.json` for global body background controls (position, size, repeat).
    - Add settings for the header menu (link color and font weight).
    - Ensure IDs are descriptive and follow existing conventions (e.g., `body_bg_position`).
  </action>
  <verify>Check settings_schema.json for newly added JSON blocks.</verify>
  <done>Settings exist in Theme Editor configuration.</done>
</task>

<task type="auto">
  <name>Refactor custom.css to use new settings</name>
  <files>assets/custom.css</files>
  <action>
    - Update `assets/custom.css` to use CSS variables derived from the new schema settings.
    - Remove `!important` tags from the `body` and `.header__inline-menu` selectors.
    - Resolve the `!important` overrides in the footer-wrapper section by using more specific CSS or standard classes.
  </action>
  <verify>Grep assets/custom.css for !important and verify that only standard utilities remain.</verify>
  <done>Zero hardcoded overrides in custom.css.</done>
</task>

## Success Criteria
- [ ] `assets/custom.css` contains zero instances of `!important` for layout or color overrides.
- [ ] All visual branding elements (colors, background behavior) are controlled via the Theme Editor.
